{
  "name": "wt-dashboards",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "npx vercel dev",
    "deploy": "npx vercel --prod",
    "lint": "node -e 'require(\"./api/data.js\"); require(\"./api/ingest.js\"); require(\"./api/health.js\"); console.log(\"All API routes load OK\")'",
    "test-ingest": "curl -s -X POST -H 'Content-Type: application/json' -H 'X-API-Key: changeme' -d '{\"date\":\"2025-01-01\",\"sites\":[],\"day_of_week\":3,\"is_weekend\":false}' http://localhost:3000/api/ingest | node -e 'process.stdin.pipe(process.stdout)'",
    "test-health": "curl -s http://localhost:3000/api/health | node -e 'process.stdin.pipe(process.stdout)'"
  },
  "dependencies": {
    "@vercel/kv": "^2.0.0",
    "nodemailer": "^6.9.0"
  }
}
